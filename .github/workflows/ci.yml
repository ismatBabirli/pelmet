# CI is disabled while the repo is private: GitHub bills macOS runners
# against included minutes at a 10x multiplier. Actions are free once the
# repo is public — uncomment everything below (and the CI badge in
# README.md) when Pelmet goes open source.
#
# name: CI
#
# on:
#   push:
#     branches: [main]
#   pull_request:
#     branches: [main]
#
# jobs:
#   build:
#     runs-on: macos-15
#     steps:
#       - uses: actions/checkout@v4
#       - name: Swift version
#         run: swift --version
#       - name: Build
#         run: swift build
